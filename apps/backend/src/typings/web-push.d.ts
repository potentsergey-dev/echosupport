// web-push is an optional runtime dependency — not listed in package.json.
// This stub allows TypeScript to compile when the package is absent.
// The actual import is wrapped in a try/catch in operator-notifier.ts.
declare module 'web-push';
